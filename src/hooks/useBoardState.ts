/**
 * Board state reducer from SSE events.
 * Manages the full dashboard state (projects, pipelines, beads)
 * and updates it in response to SSE events.
 *
 * Implementation pending — bead opencode-dashboard-5cd.
 */
